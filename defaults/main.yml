# Standards: 0.1
---
# @var istat_menus_user:description: User to run user-specific commands
# @var istat_menus_user:value: istat_menus_user | default(homebrew_user) | default(ansible_user_id)
# istat_menus_user:

# @var istat_menus_started:description: Start in background after install
istat_menus_started: True

...
